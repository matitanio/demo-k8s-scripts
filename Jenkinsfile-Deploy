pipeline {

    agent any
    stages {
        stage('Deploy'){

            steps{
            	def currentVersion = sh script: 'helm search local/demo | cut -f2 | sed -n 2p',returnStdout: true
                sh "helm upgrade --install demo local/demo-k8s-${currentVersion}.tgz"
            }
        }
    }
}
